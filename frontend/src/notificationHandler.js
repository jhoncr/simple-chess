
export async function mark_seen({id,member}) {
    console.log('seen new move of game',id,member)
    // const isGameOver = chess.game_over()
    // if (gameRef) {
    //     const updatedData = { gameData: fen_str || chess.fen(), pendingPromotion: pendingPromotion || null }
    //     console.log({ updatedData })
    //     if (reset) {
    //         updatedData.status = 'over'
    //     }
    //     await gameRef.update(updatedData)
}